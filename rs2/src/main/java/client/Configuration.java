package client;

public class Configuration {
    /**
     * This causes game to draw everything to:
     * GameShell.image
     * NOTE: must be set before initApplication
     */
    public static boolean INTERCEPT_GRAPHICS = false;
}
