package meteor.context.platform.headless

import jagex2.graphics.PixMap
import meteor.context.PlatformContext

class HeadlessPixMap(width: Int, height: Int, ctx: PlatformContext) : PixMap(width, height, ctx)
